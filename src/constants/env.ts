const { SERP_APIKEY = '', WP_CLIENT = '', WP_SECRET = '', WORDPRESS_PASSWORD = '' } = process.env

export const config = {
    SERP_APIKEY,
    WP_CLIENT,
    WP_SECRET,
    WORDPRESS_PASSWORD,
}
